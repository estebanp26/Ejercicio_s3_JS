// vite.config.js
// Configuración básica de Vite para la SPA
import { defineConfig } from 'vite'

export default defineConfig({
  // El servidor de desarrollo correrá en el puerto 5173 por defecto
  server: {
    port: 5173
  }
})
