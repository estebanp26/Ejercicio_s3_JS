{
  "name": "project-manager-spa",
  "version": "1.0.0",
  "description": "Internal project management SPA with role-based access control",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "server": "json-server --watch db.json --port 3000",
    "start": "concurrently \"npm run server\" \"npm run dev\""
  },
  "devDependencies": {
    "vite": "^5.0.0"
  },
  "dependencies": {
    "concurrently": "^8.2.0",
    "json-server": "^0.17.4"
  }
}
